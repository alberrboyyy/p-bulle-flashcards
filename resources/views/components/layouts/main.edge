<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>
      {{ title || "Flashcards" }}
    </title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  <body>
    <nav>
      <h1>
        Flashcards
      </h1>
      {{-- Le bouton Déco ira ici plus tard --}}
    </nav>

    <main>
      {{-- Cette balise magique indique où insérer le contenu des pages --}}
      {{{ await $slots.main() }}}
    </main>
  </body>
</html>
